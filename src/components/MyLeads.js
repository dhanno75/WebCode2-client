import React from "react";

const MyLeads = () => {
  // const dispatch = useDispatch();
  // const leads = useSelector((state) => state.leads);
  // console.log(leads);

  // useEffect(() => {
  //   dispatch(getUserLeads({ id: leads }));
  // }, []);
  return <div>MyLeads</div>;
};

export default MyLeads;
